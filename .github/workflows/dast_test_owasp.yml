name: Multi-Branch Security Scan

on:
  push:
    branches: ["**"]  # Todas as branches
  pull_request:
    branches: ["**"]  # Todos os PRs
  workflow_dispatch:
    inputs:
      skip_sonar:
        description: 'Skip SonarCloud analysis'
        required: false
        default: 'false'
        type: boolean

env:
  DOCKER_IMAGE_NAME: library-api
  API_PORT: 5000
  PROJECT_DIR: ./LibraryOnlineRentalSystem

jobs:
  security-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    
    strategy:
      fail-fast: false  # Continua mesmo se uma parte falhar

    steps:
      # 1. Checkout
      - name: Checkout
        uses: actions/checkout@v4.1.1
        with:
          fetch-depth: 0  # Busca histórico completo para SonarCloud

      # 2. Setup .NET
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.x

      # 3. Display branch information
      - name: Display Branch Info
        run: |
          echo "🌿 Branch: ${{ github.ref_name }}"
          echo "🔄 Event: ${{ github.event_name }}"
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "📋 PR #${{ github.event.pull_request.number }}: ${{ github.head_ref }} → ${{ github.base_ref }}"
          fi

      # 4. Build Docker
      - name: Build Docker Image
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          echo "🔨 Building Docker image..."
          docker build -t ${{ env.DOCKER_IMAGE_NAME }} .

      # 5. DAST Security Scan
      - name: DAST Security Scan
        id: dast
        run: |
          echo "🛡️ Starting DAST Security Scan..."
          
          # Create network and st