name: .NET SAST with SonarClouD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  sonarqube:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Install SonarScanner for .NET
        run: dotnet tool install --global dotnet-sonarscanner

      - name: Start SonarCloud Analysis
        working-directory: ./LibraryOnlineRentalSystem
        run: |
          dotnet sonarscanner begin \
            /k:"jorgealmeidadeveloper" \
            /o:"jorgealmeidadeveloper" \
            /d:sonar.login="${{ secrets.SONAR_TOKEN }}"

      - name: Restore dependencies
        working-directory: ./LibraryOnlineRentalSystem
        run: dotnet restore LibraryOnlineRentalSystem.sln

      - name: Build
        working-directory: ./LibraryOnlineRentalSystem
        run: dotnet build LibraryOnlineRentalSystem.sln --no-incremental

      - name: Check MSBuild version
        run: dotnet msbuild -version

      - name: End SonarCloud Analysis
        working-directory: ./LibraryOnlineRentalSystem
        run: |
          dotnet sonarscanner end \
            /d:sonar.login="${{ secrets.SONAR_TOKEN }}"

      - name: Generate JSON report from SonarCloud API
        run: |
          curl -s -u "${{ secrets.SONAR_TOKEN }}:" \
          "https://sonarcloud.io/api/issues/search?componentKeys=jorgealmeidadeveloper" \
          -o sonar-report.json

      - name: Upload Sonar report
        uses: actions/upload-artifact@v4
        with:
          name: sonar-report
          path: sonar-report.json
